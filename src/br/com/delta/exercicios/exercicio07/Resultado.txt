Resposta:

 * B) pacey pacey 4-beat

 B is correct. The tolt() method’s first two invocations come from the go() method, and  the object type determines which (overridden) method is used at runtime.

 A, C, and D are incorrect based on the above. E and F are incorrect. The casts are legal.


Saída da execução:

pacey pacey 4-beat
Process finished with exit code 0